# === Core Python ML Libraries ===
numpy==1.24.3
pandas==1.5.3
scikit-learn==1.2.2
xgboost==1.7.6

# === Deep Learning (GPU enabled) ===
tensorflow==2.12.0  # Works well on CUDA 11.2+ (for LSTM)
keras==2.12.0

# === Prophet for Time Series ===
prophet==1.1.2  # For Linux, on Windows use cmdstanpy alternative if issues

# === NLP for Sentiment ===
nltk==3.8.1
vaderSentiment==3.3.2

# === Utilities ===
requests==2.31.0
joblib==1.3.2
python-dateutil==2.8.2
pytz==2023.3
Flask==2.2.5

# === Plotting / Debugging (Optional) ===
matplotlib==3.7.1



⚙️ GPU Setup Notes
🔹 For CUDA:
Make sure your environment has CUDA 11.2+ and cuDNN installed

In Vast.ai, use a base image like:

bash
Copy
Edit
pytorch/pytorch:2.0.1-cuda11.7-cudnn8-runtime
🔹 For Prophet (Linux):
Install dependencies before Prophet:

bash
Copy
Edit
apt-get install -y build-essential python3-dev libpystan-dev
🧪 Install via virtualenv:
bash
Copy
Edit
python3 -m venv venv
source venv/bin/activate
pip install -r requirements_gpu.txt
python -m nltk.downloader vader_lexicon